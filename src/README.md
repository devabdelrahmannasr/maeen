# Source Area

Application code will live here after `RES-004` selects the implementation stack.

Do not create a speculative framework scaffold. The expected boundaries are UI shell, browser adapter, book/goal/protocol domains, deterministic rules engine, planning/snapshots, session/timer engines, local repositories, settings, and backup/import/export.

