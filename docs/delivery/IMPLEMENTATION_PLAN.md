# Implementation Plan

The detailed task plan lives in Notion. This sequence preserves its major dependencies.

1. Complete Design Freeze: `RES-004`, accessibility acceptance criteria, native Figma gaps when available, and source reconciliation.
2. Scaffold Manifest V3 extension, Side Panel, build/test pipeline, localization, theme, and accessibility baseline.
3. Implement IndexedDB schema, repositories, Chrome settings adapter, migrations, and seeded fixtures.
4. Implement goal catalog, protocol catalog, deterministic decision table, explanation output, and Protocol Snapshot.
5. Implement the ten-screen primary path with the session state machine and absolute-time timer.
6. Implement interruption recovery, abandon behavior, distraction events, and persistence boundary tests.
7. Implement progress and learning-artifact queries.
8. Implement JSON/Markdown export, guarded import, backup, and deletion.
9. Run accessibility, RTL, responsive, browser-permission, privacy, migration, and recovery checks.
10. Conduct the planned research, resolve findings, and advance through Alpha, Beta, and public-launch gates.

Each step must be broken into one-to-two-day work items in Notion and respect the WIP limit of one executable item.

