# Scripts Area

Place repeatable local setup, validation, packaging, and release-check scripts here after the toolchain is selected. Scripts must be non-destructive by default and document prerequisites and expected output.

