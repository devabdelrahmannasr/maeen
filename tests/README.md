# Tests Area

Automated tests will live here or in the stack's conventional colocated structure after `RES-004`. Preserve the strategy in `docs/delivery/TEST_STRATEGY.md` and prioritize domain/state/persistence/recovery coverage.

